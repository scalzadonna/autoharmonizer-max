# Ralph history — one line per iteration: breakthroughs, blockers, learnings.
# Newest at the bottom. A fresh agent reads the tail before acting.

2026-07-14  setup  Scaffolded Ralph (CONSTITUTION, PROMPT, loop, spec 0001).
2026-07-14  setup  Sequencer fold-in is in the working tree, UNCOMMITTED on max-markov; spec 0001 lands it on branch ralph/build and adds chord_parser tests (port from max-markov-2, 61 assertions, `node chord_parser.test.js`).
2026-07-14  env    pytest python = /opt/anaconda3/bin/python3 (system python3 lacks pytest). Ports 9000/9001 must be free. Shared markov_osc.js drives BOTH devices — do not regress the generator (gate on player.active). Ableton audio is human-only.
2026-07-14  setup  Branch model: ALL specs stack on ONE branch `ralph/build` (create off max-markov on iter 1, then commit onto it) — NOT per-spec branches, so later specs see earlier specs' code. Backlog: 0001 land sequencer + parser tests; 0002 sequencer-engine unit tests; 0003 sequencer MIDI-thru parity; 0004 docs sync (osc_contract + PLAN, contract⇄Python drift check).
2026-07-14  0001   DONE (executed under supervision, not via loop). Branch ralph/build created off max-markov. Sequencer device + unified bridge committed (1e65b56); chord_parser.test.js ported from max-markov-2 — 466/466 pass against the repo's superset parser, npm test wired (22ccd77). Generator patch untouched; node --check clean; generator stub harness 22/22. Next: 0002.
2026-07-14  0002   DONE. Ported the dev stub harness to max/markov_osc.test.js (22 assertions: generator-immediate, sequencer-hold-on-beat, density gating 8-vs-1, dial mapping, downbeat-quantized sweep, v3 OSC). npm test now runs both suites (466+22, exit 0). Next: 0003.
2026-07-14  0003   DONE. Added gated MIDI thru to the sequencer maxpat (midiin -> gate -> midiout, toggle + loadmess 1 default-on), a 1:1 clone of the generator's thru; +5 boxes/+4 lines, 150-line additive diff. Adversarially verified (2 lenses PASS: Max gate/toggle semantics + isolation/thru-only-no-seed). .amxd rebuilt (112 boxes), npm test 466+22 green, generator patch + bridge untouched. Next: 0004.
2026-07-14  0004   DONE. Docs synced to two-device reality: osc_contract.md + PLAN.md name chord_sequencer_device; documented internal (non-OSC) Max message vocabulary; completed the debug-address tables (added /debug/model + /debug/session_history — both docs were stale). Added python/scripts/check_osc_contract.py (config.py<->doc drift check): 18==18, no drift. npm test 466+22 green; pytest 21 passed/64 skipped. ALL SPECS COMPLETE.
2026-07-21  0005   DONE. Fit Generator panel to M4L width: narrowed 5 over-wide right-column readouts (obj-status/model-display/session-display 270->168, obj-output/error 340->236); max right edge 740->638, max bottom 312, single page (no live.tab needed). Model selector (obj-model-menu markov/rnn/lstm -> prepend model -> node; route ...model) untouched; markov_osc.js + python/src unchanged (surgical 5-width diff). .amxd rebuilt (params Rhythm, Spice); npm 466+22 green; pytest 85 passed/0 skipped (rnn+lstm checkpoints placed so neural tests now run). Ableton visual fit = human-only, not gated. ALL SPECS COMPLETE.
2026-07-21  0006   DONE. Sequencer model selector: added obj-seq-model-menu (umenu markov/rnn/lstm) -> obj-seq-prepend-model (prepend model) -> obj-node, mirroring the generator; extended obj-route with a trailing `model` outlet (numoutlets 9->10, old unmatched outlet 8 was unused so no re-index) -> obj-seq-model-disp readout; all in the free right column (max right edge 496, no overlap). markov_osc.js + generator patch + python untouched (JSON round-trip is byte-identical so the diff is purely additive). .amxd rebuilt (117 boxes/97 lines, params Rhythm/Major/Minor/Seventh); npm 466+22, pytest 85/0. Ableton listen = human-only. ALL SPECS COMPLETE.
2026-07-21  0007   DONE. Sequencer session + temperature (Spice) controls, parity with the generator: obj-seq-session-menu (auto/stateless/session) -> prepend session -> node; reset button -> reset_session -> node; obj-seq-dial-spice (live.dial param Spice) -> prepend spice -> node; obj-route extended with a trailing `session` outlet (numoutlets 10->11) -> obj-seq-session-disp. New right column x510-630 (max right edge 630, no overlap). All UI-only: /control/session, /control/spice + bridge handlers already existed; markov_osc.js + generator + python untouched (additive JSON round-trip diff). .amxd rebuilt (127 boxes/104 lines, params +Spice); npm 466+22, pytest 85/0. Ableton listen = human-only. ALL SPECS COMPLETE.
